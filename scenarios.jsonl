{"id":"public-baseline-build","kind":"local_artifact","command":"npm run build:packet && npm run validate && npm run dashboard:build","requires":[],"expected":"A public_validation_baseline packet for exactly three validated Goodwin profiles; Context operations are blocked_missing_credentials or proposed and the dashboard build succeeds."}
{"id":"contextdev-three-profile-live","kind":"external_api","command":"npm run context:live","requires":["CONTEXT_DEV_API_KEY","Context.dev private-alpha people/retrieve access"],"expected":"Three isolated POST /people/retrieve attempts with saved per-profile receipts; no token or contact fields appear in public output."}
